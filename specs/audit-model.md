# Audit Model

The operator should always be able to answer:

- what remote source directory was processed
- when it was processed
- which run produced the outputs
- whether upload completed
- which files were exceptional
- which local artifacts were intentionally deleted

Required retained artifacts:

- remote input pointer
- run manifest
- run log
- final upload verification summary
- exceptional file reports
