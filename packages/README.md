# Packages

This mono-repo is split into three logical packages:

- `infoproc-core`
- `bdpan-ops`
- `integration`

Each package is intentionally small at this stage and exists to make the future
open-source split explicit.
