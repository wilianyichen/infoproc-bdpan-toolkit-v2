# Release Examples

Files in this directory describe the output shape of generated release bundles.

- `release_manifest.example.json`
  example metadata emitted by `scripts/release/package_release.py`

These files are intended for CI checks, audit tooling, and operator reference.
